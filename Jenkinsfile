pipeline{
    agent any

    stages{
        stage("Build the java project"){
            steps{
            echo message: "--------------Building the webapp project---------"
            }
        }
    }
}